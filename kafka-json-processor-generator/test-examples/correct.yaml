name: "Example processor"
streams:
  - input_topic: in
    output_topic: out

    processors:
      - kind: static_field
        field: hello
        value: world
      - kind: copy_field
        source_field: abc
        target_field: def
